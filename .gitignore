__pycache__/
*.py[co]
.venv/
dist/
.pytest_cache/
.complexipy_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/
tmp/
# cosmic-ray mutation session databases. The `mutate` recipes write the
# work order plus per-mutant results here; it is regenerated each run.
.cosmic-ray/
COMMIT_AGENTMSG
src/proofhouse_python_tool/_buildstamp.py

# Vale synced packages (keep the vocabulary).
# Each negation needs the parent un-ignored first — git can't re-include
# a child whose ancestor directory stays excluded.
.vale/*
!.vale/config/
.vale/config/*
!.vale/config/vocabularies/
.vale/config/vocabularies/*
!.vale/config/vocabularies/proofhouse/

# Claude Code
.claude/worktrees/
.claude/settings.local.json

# macOS
.DS_Store
